# Supabase — used by src/lib/supabase.js for auth/session.
# ChainPay works without these (the in-page wallet is self-custodial and local),
# but session-linked features (payment proofs, user reference) need them.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
